# Client-side fallback key for native builds only (optional if you proxy everything)
EXPO_PUBLIC_SPOONACULAR_API_KEY=

# Optional override. Default is /api/spoonacular.
# For native apps to use your hosted proxy, set this to full URL:
# EXPO_PUBLIC_SPOONACULAR_PROXY_URL=https://your-domain.com/api/spoonacular
EXPO_PUBLIC_SPOONACULAR_PROXY_URL=/api/spoonacular

# Server-side key used by Vercel/Netlify functions (required for web deploy)
SPOONACULAR_API_KEY=
